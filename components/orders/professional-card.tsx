import type { OrderProfessional } from "@/data/orders";

export function ProfessionalCard({ professional }: { professional: OrderProfessional }) {
  return <section className="rounded-2xl border border-line bg-white p-5 shadow-card" aria-labelledby="professional-heading"><h2 id="professional-heading" className="text-lg font-bold text-foreground">Your professional</h2><div className="mt-4 flex items-center gap-4"><div aria-hidden="true" className="grid size-14 shrink-0 place-items-center rounded-full bg-brand-soft text-xl font-bold text-brand">{professional.name.split(" ").map((part) => part[0]).join("")}</div><div><p className="font-semibold text-foreground">{professional.name}</p><p className="mt-1 text-sm text-muted">{professional.category} · ID {professional.id}</p><p className="mt-1 text-sm font-semibold text-foreground"><span className="text-[#d88400]">★</span> {professional.rating.toFixed(1)} <span className="font-normal text-muted">· Verified professional</span></p></div></div><div className="mt-5 flex flex-wrap gap-3"><button type="button" className="inline-flex min-h-10 items-center justify-center rounded-xl border border-line px-4 text-sm font-semibold text-foreground hover:border-brand hover:text-brand">Contact Support</button><button type="button" className="inline-flex min-h-10 items-center justify-center rounded-xl border border-line px-4 text-sm font-semibold text-muted hover:border-brand hover:text-brand">Call Professional</button></div></section>;
}
